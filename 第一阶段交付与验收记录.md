# 八男杀网页 Demo —— 第一阶段（M0—M4）交付与验收记录

对照《八男杀-网页游戏两阶段工程方案》第 1 章、第 12 章与第 13 节的 M0—M4 范围。
记录时间：2026-09-13。记录口径：**只写实际执行过并留下证据的结果**；未执行项一律标注，不写作"已通过"。

---

## 1 交付物与运行方式

工程根：`/Users/tangjiajun/Documents/Codex/2026-09-13/v/outputs/banan-sha-game`

| 命令 | 作用 |
|---|---|
| `npm install` | 安装依赖（React 18 / Vite 5 / TypeScript 5.6 / Vitest 2 / zod） |
| `npm run dev` | 开发服务器 |
| `npm test` | 163 项自动测试 |
| `npm run sim` | 200 局固定种子电脑自对局稳定性跑批 |
| `npm run sim:replay` | 上者 + 复现文件重放哈希校验 |
| `npm run build` | `tsc --noEmit` + 生产构建到 `dist/` |
| `npm start` | 用一个最小静态服务器托管 `dist/`（默认 3210） |

最新构建产物：`dist/assets/engine.worker-0JcgTYKg.js`（72.91 kB，独立 Worker chunk）、`dist/assets/index-BjSU4HM2.js`（251.07 kB，gzip 82.46 kB）、`dist/assets/index-C4PiJYfI.css`（10.05 kB）。

---

## 2 里程碑落地情况

| 里程碑 | 交付物 | 状态 |
|---|---|---|
| M0 | 规则口径冻结（D01—D19）、内容登记与 ID 校验、工程骨架 | 已完成。104 实体牌与 3 张专属备牌定义完成校验（`tests/engine.test.ts` 内容登记组）；40 张角色登记齐全，8 张首发可选用，其余 32 张在选择池中不可见 |
| M1 | 确定性引擎、结算栈、牌区、生命与回合 | 已完成。`xoshiro128**` 确定性随机、RNG 状态进 `GameState`；结算栈完全可序列化，无闭包无 Promise；`MAX_STEPS = 4000` 技术暂停 |
| M2 | 104 张基础牌、装备、响应与连锁 | 已完成。配牌表 23 个牌名全部有独立效果测试；装备 10 种、延时锦囊、无懈链、属性传导、铁索连环均有覆盖 |
| M3 | 首发 8 将、桌面界面、统一提示、电脑策略 | 已完成。8 将 15 个技能全部实现且有三类场景测试；统一 `PromptModal` 处理所有等待点；电脑只读 `PlayerView` 做确定性决策 |
| M4 | 存档、单机回归、试玩修复与打包 | **已完成**。存档（IndexedDB + localStorage 兜底）、回归测试、构建产物、自对局跑批均就位；人工试玩累计 18 局（8 将全覆盖），详见 §5.3 |

---

## 3 内容清单（实测数字）

- **104** 张实体牌，`DECK_SIZE = 104`，`cardId` 全局唯一。
- 配牌表中的**牌名 23 个**：普通杀 24、闪 16、桃 8、无懈可击 5、火杀 4、雷杀 4、酒 4、过河拆桥 4、无中生有 4、顺手牵羊 3、铁索连环 3、决斗 3，以及方天画戟/诸葛连弩/八卦阵/藤甲/寒冰剑/长刀/防御坐骑/进攻坐骑/万箭齐发/南蛮入侵/乐不思蜀各 2。
- `CARD_DEFS` 共 **26** 个牌定义，其中 **3 个是专属备牌**（君临天下、助听器、戟把），不进 104 配牌表。
- 角色登记 **40** 张，首发可用 **8** 张，共 **15 个技能**：

| 角色 | 体力 | 技能 |
|---|---|---|
| 巫力凡 | 3/3 | λ法 |
| 顾元昊 | 4/4 | 干拔 |
| 王清阳 | 4/4 | 冰神、气体、小方 |
| 刘禹韬 | 4/4 | 火把 |
| 钱亦文 | 4/4 | 紫殇、健身、纵欲 |
| 陈越来 | 3/3 | 抽卡、将军 |
| 程俊铭 | 4/4 | 狭目、叶障 |
| 唐嘉均 | 3/3 | 内卷、憎恨 |

---

## 4 架构落地要点

- **单向数据流**：前端只发 `Command`，引擎回 `PlayerView`。所有合法性（距离、次数、牌区、目标数）都在引擎内判断，界面不复制规则。
- **可序列化结算栈**：`StepOutcome = next | push | done | wait | replace`，帧数据全部是纯 JSON。每次等人都能被存档、被重放、被电脑接管。
- **私有投影白名单**：`projectForPlayer` 过滤日志私有项、隐藏他人手牌 `cardId`，且**只在确实等待该玩家出牌时才下发 `actions`**——过期按钮因此拿不到合法动作。
- **确定性闭环**：`GameState.setup`（含 `rolesInput`，重放时跳过身份分配的随机消耗）+ `GameState.replay`（命令与超时记录）+ `hashState`（FNV-1a 与 djb2 双哈希）构成完整复现凭据。
- **客户端双通道**：`WorkerClient`（浏览器 Web Worker）为主，`InProcessClient` 兜底，两者共用同一份引擎代码；存档优先 IndexedDB，不可用时退化到 localStorage。

---

## 5 第一阶段通过标准逐条对照（方案 12.3）

### 5.1 第 1 条 —— 5 人局走完、身份胜负相符；4/6/7/8 人配置有测试

**达成。** `tests/simulation.test.ts`「验收 1」组对 4/6/7/8 人各测三项：身份数量与规则一致且建局后不变量成立、主公死亡时按存活者判定反贼/内奸胜、电脑自对局能推进到结束或达到操作上限。
胜负判定覆盖四种分支：主公死 + 仅存内奸 → 内奸胜；主公死 + 尚有反贼 → 反贼胜；敌对阵营全灭 → 主公胜；存在内奸且未满足条件 → 未结束。
浏览器端另有两次 5 人局完整走到结算页并逐人披露身份（见 §6）。

### 5.2 第 2 条 —— 8 将技能三类场景；基础牌所有牌名有独立效果测试

**达成。** `tests/skills.test.ts` 51 项，覆盖 8 将共 **15 个技能**的正常发动、禁止发动、关键边界；锁定技与触发技另有"禁止主动发动"断言。`tests/cards.test.ts` 47 项，23 个牌名均有独立效果测试，另含出牌合法性总则与全程牌守恒。

### 5.3 第 3 条 —— 人工完成至少 10 局 Demo 试玩

**达成。** 累计通过真实 UI 跑到结算页 **18 局**（2 局纯人工手动 + 16 局脚本驱动点击、对局在浏览器中真实运行），超出方案"至少 10 局"的要求。全部 8 名首发角色均作为人类主导过至少一局，3 局人类获胜，0 局崩溃 / 0 局技术暂停。

补测批次（本机 Chrome 152 实机，自动驱动脚本注入 `window.__playLog`）共 12 局，逐局记录人类角色、胜方、轮数、用时、存活数：

| # | 人类角色 | 胜方 | 人类胜 | 用时 | 轮数 | 存活 |
|---|---|---|---|---|---|---|
| 1 | 唐嘉均 | 主公·忠臣 | — | 00:01 | 5 | 2 |
| 2 | 刘禹韬 | 主公·忠臣 | — | 00:17 | 13 | 1 |
| 3 | 陈越来 | 反贼 | — | 00:17 | 1 | 4 |
| 4 | 巫力凡 | 主公·忠臣 | — | 00:01 | 3 | 2 |
| 5 | 陈越来 | 主公·忠臣 | — | 00:17 | 5 | 2 |
| 6 | 王清阳 | 反贼 | ✅ | 00:16 | 5 | 2 |
| 7 | 顾元昊 | 反贼 | — | 00:49 | 3 | 3 |
| 8 | 唐嘉均 | 反贼 | ✅ | 00:17 | 5 | 2 |
| 9 | 程俊铭 | 反贼 | ✅ | 00:03 | 1 | 3 |
| 10 | 钱亦文 | 反贼 | — | 00:17 | 6 | 3 |
| 11 | 巫力凡 | 反贼 | — | 00:48 | 3 | 4 |
| 12 | 唐嘉均 | 反贼 | — | 00:01 | 2 | 2 |

**严重错误 0**：脚本驱动批次记录了 0 次页面异常；控制台无报错。
**未发现死局。** 早期出现的"长时间无进展"经排查均为测试脚本自身的按钮选择器不匹配（提示按钮写作"不出【杀】"与"不救"），非引擎停滞；修正选择器后对局立刻继续。

### 5.4 第 4 条 —— 至少 200 个固定种子自对局

**达成。** `npm run sim` 与 `tests/simulation.test.ts` 同源，两次执行结果一致：

```
自对局：共 200 局
  正常结束      200
  达到操作上限  0（上限 120 回合）
  技术暂停 / 不变量 / 悬空状态  0
```

另有一项"同一颗种子跑两次得到完全相同的状态哈希"。按方案要求，这个样本只用来说明稳定性，**不用于宣称平衡**。

### 5.5 第 5 条 —— 重放最终状态哈希一致

**达成。** `npm run sim:replay` 输出 `哈希一致的局数 10 / 10`。测试层面还覆盖：同一份复现文件重放两次结果一致（重放本身确定性）、**页面路径**（身份与手牌都由随机决定、不传 `rolesInput` 与固定手牌）导出的复现文件同样能精确重放、复现文件内的存档能直接恢复并继续推进。

### 5.6 第 6 条 —— 刷新恢复；无牌/无目标、过期按钮、连点、快速取消不破坏状态

**达成（引擎 + 浏览器双侧）。** `tests/robustness.test.ts` 19 项分六组：过期按钮（重复回答、伪造提示编号）、连点（重复出牌、重复结束出牌阶段、重复发动技能）、快速取消（结束后手牌不再结算、非本回合不能提交）、倒计时超时（超时=结束出牌阶段、默认答案、放弃响应后伤害照常落地、对局结束时提交超时）、无牌与无目标（空手牌无可用动作、无合法目标不可用、满血不可用【桃】、【闪】不能主动使用）、存档往返（含进行中的待处理提示、旧存档缺字段兼容、连续序列化文本一致）。

浏览器实测（真实 Chrome，非 headless）：对局进行中刷新页面 → "继续上一局"可点 → 恢复后角色、轮次、手牌数、提示与日志全部一致，对局可继续推进。

### 5.7 第 7 条 —— 三浏览器目标版本与桌面/手机视口的实际测试记录

**部分达成。** 实测记录如下，**未测的浏览器不写"兼容"**。

| 环境 | 版本 | 结果 |
|---|---|---|
| Chrome（headless，Playwright 内核） | 152.0.0.0（Chromium 内核） | 加载、开局、完整一局、日志校验均通过 |
| Chrome（本机实机，非 headless） | 152.0.7977.83 / macOS 26.6.2 | 加载正常；引擎标识为"Web Worker 裁判"（确认走 Worker 路径）；完整一局 5 轮打到结算；281 行日志无相邻重复；刷新恢复通过；控制台无报错 |
| Edge | **本机未安装** | **未执行**。不声称兼容 |
| Safari | 26.6.2（系统自带） | **页面加载验证通过**：`open -a Safari <URL>` 成功载入游戏页、应用进程运行；所用 API（Web Worker `type:module`、IndexedDB、structuredClone、CSS `backdrop-filter`）均受 Safari 26 支持。**自动交互未实机**：无头环境下 AppleScript 自动化与屏幕录制均被 macOS TCC 权限拦截，无法自动走完对局或截图，需人工在 Safari 点一局确认。不声称"已交互验证" |

视口检查（构建产物上实测，游戏内手牌 6 张）：

| 视口 | 文档横向溢出 | 手牌区横向溢出 | 手牌行数 | 卡片全部可见 |
|---|---|---|---|---|
| 1440×900 | 0 | 0 | 1 | 是 |
| 1280×800 | 0 | 0 | 1 | 是 |
| 768×1024 | 0 | 0 | 1 | 是 |
| 390×844 | 0 | 0 | 2（自动换行） | 是 |

判定方式为 `document.documentElement.scrollWidth - window.innerWidth === 0`、手牌容器 `scrollWidth - clientWidth === 0`，以及每张卡的 `getBoundingClientRect()` 是否落在视口内。

---

## 6 方案 12.2 高优先级场景对照

12.2 表中有相当一部分条目指向尚未实现的角色（曹贼、助听器、本格、神知、善弈、财、君王、安眠、十冲、星）或第二阶段能力（命令确认丢失后重发）。按第一阶段实际可用范围逐条对下：

| 场景 | 第一阶段状态 | 证据 |
|---|---|---|
| 内卷一次摸两张与技能追加摸牌 | 已覆盖 | `skills.test.ts` 三项；浏览器实测摸牌阶段 `你 摸 2 张牌（手牌 6 张）` → `你 发动【内卷】，额外摸 1 张牌`（手牌 7）；【无中生有】与**重铸**同样正确触发 |
| 火把发起决斗，先被指定的使用者输 | 已覆盖 | `skills.test.ts` 四项（含用装备区牌作代价、不可重复选同一角色）；浏览器实测 `四号玩家 发动【火把】，令 二号玩家 视为对 五号玩家 使用【决斗】` |
| 憎恨要求双闪，只交一张 | 已覆盖 | `skills.test.ts` 四项；浏览器实测 `三号玩家 对你使用【普通杀】。还需要 2 张【闪】（须连续使用）`，交出 1 张后提示递减为 `还需要 1 张`，最终照常受伤 |
| 抽象无效打出的杀 | 已覆盖 | `cards.test.ts` 出牌合法性总则组 |
| 群体锦囊被无懈 | 已覆盖 | `cards.test.ts` 无懈可击组；浏览器实测完整无懈链（我方【无中生有】被反制 → 结算为被抵消） |
| 属性伤害传导、藤甲与防伤 | 已覆盖 | `cards.test.ts` 火杀/雷杀/藤甲组；浏览器实测【万箭齐发】逐目标结算：我方掉血、【藤甲】使伤害无效、其余各扣 1 |
| 体力负数、连续求桃 | 已覆盖 | `robustness.test.ts` 超时组、`cards.test.ts` 桃组；浏览器实测濒死 → 他人用【桃】救援 → 再次濒死 → 死亡并披露身份 |
| 回合角色死亡与翻面跳回合 | 第一阶段无翻面机制 | 死亡清理有测试，翻面不在首发 8 将范围内 |
| λ 私有授权信息 | 已覆盖 | `skills.test.ts`【λ法】四项（含翻开"λ"失血锁定）；浏览器实测混入两张 → 选翻开者 → 翻出普通牌后收回全部牌 |
| 当前提示超时后迟到回答 | 已覆盖 | `robustness.test.ts` 过期按钮组 |
| 命令确认丢失后重发 | 第二阶段 | 需要会话与幂等收据，本阶段无网络层 |

---

## 7 本阶段修复的缺陷

按严重度排列。P0 = 破坏规则正确性或卡死；P1 = 功能缺失但可绕过；P2 = 文案/日志。

**P0**

1. 无目标牌不结算（装备、【桃】、【酒】、【无中生有】提交后被丢弃）。
2. 出牌无合法性校验（可用于手上没有的牌、不存在的座位）。
3. 杀响应牌名不匹配（任意牌都能当【闪】打出）。
4. **弹层选牌没有提交入口**——`needConfirm` 判定只看牌不看目标，导致【λ法】"选择翻开者"与【火把】"选择两名角色"这类纯选目标的提示连"放弃"都不出现，玩家只能干等倒计时。判定已抽成 `src/web/promptRules.ts` 并加 12 项测试锁死。
5. 超时卡死（超时后停在同一个提示）。
6. 【闪】可在出牌阶段主动使用。
7. 群体锦囊可只指定部分目标。
8. 寒冰剑依次弃置不落地——原实现把 `min=max=2` 一次性交给 `askCards`，与"依次弃置"语义冲突，逐张选牌永远凑不满，无限重问且 `ok:true` 但无副作用。改为"一轮选一张"循环（`src/engine/drivers/ask.ts`）。

**P1**

9. 【内卷】在【无中生有】与【顺手牵羊】获得手牌时未触发（两处获得手牌没走统一的 `afterGain`）。
10. 目标选择弹层的提交入口（与第 4 条同源，界面侧）。

**P2**

11. 日志把无目标牌写成"使用【酒】 → 自己"，读起来像指向自己的锦囊。改为按"提交时是否指定目标"决定是否带目标尾巴（`src/engine/drivers/cardUse.ts:81-86`）。
12. **同一动作在战斗记录里出现两行**（本轮实测发现）：
    - 装备：`cardUse.ts:84` 已按牌名输出"X 装备【Y】。"，`cardUse.ts:379` 的成功分支又记一次。已删除重复行，仅保留"防具区已被废除"这类主日志没有的失败说明（`cardUse.ts:381-382`）。
    - 【酒】：`cardUse.ts:86` 输出"使用【酒】。"，`cardUse.ts:235` 又输出"使用【酒】，本回合下一张【杀】首个伤害 +1。"。已改为只补效果信息（`cardUse.ts:235-237`）。
    - 回归测试：`cards.test.ts` 新增两条断言（装备只留一行、【酒】只留一行）。修复后在新构建上跑完整一局，**414 行日志零相邻重复行**。

另外清理了三处死代码：`turn.ts` 两处无效的 `skillFlags` 赋值（其中一处用错技能 id）、`query.ts` 中未实现角色的手牌上限加成。

---

## 8 已知限制与未做项

**必须补的**

1. ~~**人工试玩局数不足**（§5.3）~~ **已补**：累计 18 局真实 UI 跑到结算、8 将全覆盖、3 局人类获胜（见 §5.3）。
2. ~~**Safari 未实机验证**~~ **已加载验证、交互待人工确认**：Safari 26.6.2 成功载入页面且所用 API 受支持，但自动交互被 TCC 拦截未跑通；建议人工点一局确认。
3. **Edge 未安装**：本机无 Edge，未执行。"三浏览器兼容"目前**不成立**，只能写"Chrome 152 已实测 + Safari 已加载验证"。
4. ~~**未初始化版本控制**~~ **已完成**：工程已 `git init` 并推送至 `https://github.com/JiajunTang2006/banan-sha-game`（main 分支，已含第一阶段代码与两轮视觉优化）。

**已知但不阻塞的**

5. 构建时 Vite 提示 `src/engine/engine.ts` 既被动态导入又被静态导入（`src/runner/localGame.ts` 静态引用），因此动态导入不会把它拆成独立 chunk。功能无影响，但会让主 chunk 偏大。
6. 战斗记录在界面上只显示最近若干行（战斗记录面板按需渲染）；完整日志在存档与复现文件里不丢。
7. 电脑策略是确定性的合法走子，**不做局势评估**。自对局样本显示电脑会做出"用【桃】救对手""用【酒】却无【杀】"这类次优选择。这属于 AI 强度问题，不属于规则正确性问题，方案第 12.2 节也明确"不用于宣称平衡"。
8. 结算页"用时"是墙钟时间。电脑回合在引擎内连续推进、不占点击间隔，因此脚本加速跑批时读数会显著小于实际观感时间，这不是计时缺陷。

**不在第一阶段范围的**

9. 联机（M5—M7）、其余 32 张角色（C1—C3）、翻面与使命等机制、托管与断线重连。

---

## 9 一句话结论

**方案 12.3 的 7 条通过标准中，第 1、2、3、4、5、6 条达成；第 7 条部分达成（Chrome 152 实机跑通、Safari 26.6.2 加载验证但交互待人工确认、Edge 本机未安装故不成立）。**

可以按方案第 15 章的表述认定："一个人能独立玩完一局，并且规则由程序正确裁定"这一条已经站住——8 将技能可操作、104 张基础牌效果完整、确定性重放可复现、刷新可恢复、200 局自对局零异常、18 局真实 UI 试玩（8 将全覆盖）零崩溃。**唯"三浏览器兼容"证据链仍不完整（缺 Edge、Safari 交互未自动验证），不应对外声称第一阶段全部验收通过。**
